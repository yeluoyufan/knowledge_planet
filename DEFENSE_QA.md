# 垂直领域专业知识社区 - 答辩常见问题总结 (Q\&A)

本指南旨在帮助您应对毕业设计答辩。问题侧重于**系统架构、功能逻辑、业务处理**以及**设计决策**，较少涉及底层代码细节。

***

## 一、 系统架构与选型

### 1. 为什么要采用前后端分离的架构？

- **回答思路：**
  - **解耦**：前端（Vue 3）专注于用户界面与交互，后端（Spring Boot 3）专注于业务逻辑与数据处理，职责清晰。
  - **效率**：开发时可以前后端并行，接口通过 JSON 通信。
  - **性能**：前端可以使用浏览器缓存，减轻服务器压力；后端只提供 API，响应更快。
  - **扩展性**：未来如果需要开发移动端 App 或小程序，可以复用同一套后端 API。

### 2. 系统的权限模型（RBAC）是如何设计的？

- **回答思路：** 系统采用了 **RBAC (Role-Based Access Control)** 基于角色的访问控制模型。
  - **角色划分**：
    - **超级管理员**：负责全站治理（如增删板块、封禁用户、管理所有标签）。
    - **版主**：负责特定板块的运营（如审核该板块帖子、置顶、删除违规内容）。
    - **普通用户**：进行内容的创作、阅读、点赞、收藏及互动。
  - **实现方式**：后端使用 Spring Security 配合自定义切面（如 `@RequireAdmin`）在接口层进行拦截。

***

## 二、 核心功能逻辑

### 3. 为什么设计“已发布帖子修改后必须重新进入审核”的流程？

- **回答思路：**
  - **内容安全**：这是垂直专业社区的核心质量管控手段。防止用户在通过审核后，恶意篡改内容（例如加入广告、色情、诈骗信息等）。
  - **一致性**：确保用户在首页看到的、搜索到的内容始终是经过平台认可的专业内容。

### 4. 系统的搜索功能是如何实现的？有哪些增强？

- **回答思路：**
  - **实现**：基于数据库的模糊匹配，但进行了多维度增强。
  - **维度**：支持按标题、正文、标签以及作者昵称进行联合检索。
  - **体验**：搜索结果页与首页样式高度统一，支持在结果中按热度、时间二次排序，提升了知识检索的精准度。

### 5. 消息通知系统目前是如何运作的？

- **回答思路：**
  - **机制**：采用了**前端轮询**机制。前端每隔 1.5 秒向后端请求一次未读数统计。
  - **覆盖场景**：涵盖了点赞、收藏、评论回复、系统通知（审核结果、置顶提醒）等全场景。
  - **分类**：在消息中心对消息进行了精细化分类（如“收到的赞”、“回复我的”），提升用户处理信息的效率。

***

## 三、 数据处理与一致性

### 6. 帖子标签（Tags）是如何存储的？为什么不使用传统的关联表？

- **回答思路：**
  - **设计**：采用了**非范式化存储**，将标签列表以 **JSON 字符串**形式直接存在 `forum_post` 表的 `tags` 字段中。
  - **理由**：
    1. **性能**：查询帖子列表时不需要通过 JOIN 关联标签表，极大减少了数据库 IO。
    2. **独立性**：垂直社区的标签通常是帖子的重要属性，随帖存储更符合“知识快照”的概念，即使后台删除了某个标签，历史帖子展示也不会受影响。

### 7. 系统是如何管理上传的图片的？（引用计数逻辑）

- **回答思路：**
  - **痛点**：防止用户上传大量垃圾图片占用服务器空间。
  - **逻辑**：系统维护了一个 `sys_file` 表，记录图片的引用计数（`ref_count`）。
    - 当帖子引用一张图，计数 +1。
    - 当用户修改帖子删除了某张图，或删除帖子时，计数 -1。
  - **清理**：系统后台有定时任务，会自动扫描计数为 0 且上传超过一定时间的冗余文件进行物理删除。

***

## 四、 业务场景模拟

### 8. 如果某个板块突然涌入大量违规内容，系统有哪些治理手段？

- **回答思路：**
  - **事前**：开启全站或该板块的“先审后发”模式。
  - **事中**：版主和管理员拥有“一键删除”权限，并可以利用“置顶”功能将官方公告置顶。
  - **事后**：超级管理员可以后台封禁违规用户账号，通过 `status` 字段切断其登录权限。

### 9. 评论系统的“楼中楼”逻辑是如何维护的？

- **回答思路：**
  - 通过 `parentId`（直接父评论）和 `rootId`（根评论）两个字段维护。
  - **优势**：无论嵌套多深，前端只需通过 `rootId` 即可一次性查出该评论下的所有互动，再在内存中进行层级排序展示，避免了数据库的递归查询。

***

## 五、 系统不足与改进（必问：如果给你更多时间你会怎么优化？）

### 10. 目前系统的局限性有哪些？

- **回答思路：**
  - **实时性**：目前消息是轮询，未来应引入 **WebSocket** 实现真正的秒级实时推送。
  - **性能**：当数据量达到百万级时，数据库模糊搜索会变慢，未来应引入 **ElasticSearch** 全文搜索引擎。
  - **智能化**：目前全靠手动检索，未来可以根据用户的点赞/收藏记录，利用简单的**推荐算法**（如协同过滤）进行个性化内容推送。
  - **交互**：目前移动端是响应式适配，未来可以考虑开发专门的 **Uni-app** 或小程序版，提升触屏操作手感。

***

**答辩锦囊：**

- **不要慌**：如果老师指出了某个逻辑漏洞，先点头承认，然后解释你当时设计的出发点（为了开发效率、或者为了简化模型），最后表示这是后续优化的方向。
- **重逻辑**：老师更看重你对业务流程的掌控，而不是你背了多少代码。
- **贴合“垂直”**：时刻记住你的项目是“专业社区”，所有功能（Markdown、审核、TOC）都是为了服务于“高质量知识沉淀”。

