# 垂直领域专业知识社区 - 项目全维度结构解析手册

本手册将深入剖析项目源码的每一个角落，通过**【学术严谨版】**与**【简单易懂版】**双重维度，帮助你彻底掌握项目的工程结构。

---

## 一、 后端架构篇 (`backend/src/main/java/com/hd/forum`)

后端基于 SpringBoot 3 构建，遵循经典的“层级解耦”设计模式。

### 1. `controller/` (控制层)
*   **【学术严谨版】**：作为系统的 **RESTful 接入层**，负责处理 HTTP 请求的路由分发、媒体类型协商及响应封装。通过 `Annotation` 驱动实现声明式 API 定义，是外部世界与内部业务逻辑的唯一契约接口。
*   **【简单易懂版】**：项目的**“前台办事窗口”**。前端发来的所有请求（比如发帖、登录）都先在这里排队，它负责收下你的材料，然后告诉后面的部门去干活。
*   **核心文件**：[PostController.java](file:///d:/Code/Java/forum/backend/src/main/java/com/hd/forum/controller/PostController.java) (处理帖子相关请求)。

### 2. `service/` (业务逻辑层)
*   **【学术严谨版】**：系统的**核心领域模型处理层**。封装了原子化的业务规则，通过接口与实现类（`impl`）分离的模式，实现了业务逻辑的抽象与高度可测试性。支持事务控制与跨模块调度。
*   **【简单易懂版】**：项目的**“核心加工车间”**。所有的“骚操作”都在这里完成，比如发帖时要检查你有没有被封禁、要不要扣积分、要不要通知管理员，这些复杂的逻辑全部写在这里。
*   **核心文件**：[PostServiceImpl.java](file:///d:/Code/Java/forum/backend/src/main/java/com/hd/forum/service/impl/PostServiceImpl.java)。

### 3. `mapper/` (持久层)
*   **【学术严谨版】**：基于 **Data Access Object (DAO)** 模式，利用 MyBatis Plus 框架实现 Java 实体类与 SQL 语句的映射。负责数据库连接池的交互及结果集的 ORM（对象关系映射）转化。
*   **【简单易懂版】**：项目的**“仓库管理员”**。它只管两件事：把数据从数据库里取出来给 Service 用，或者把 Service 处理完的数据存进数据库。
*   **核心文件**：[PostMapper.java](file:///d:/Code/Java/forum/backend/src/main/java/com/hd/forum/mapper/PostMapper.java)。

### 4. `entity/` (实体类层)
*   **【学术严谨版】**：**持久化对象 (PO)**，代表了数据库模式的 Java 语言定义。通过 Lombok 注解实现样板代码的编译时注入，严格遵循数据库字段的物理定义。
*   **【简单易懂版】**：项目的**“登记簿模版”**。每一行代码都代表数据库表里的一个列名。你想存个帖子，系统就得按照这个模版来填空。
*   **核心文件**：[Post.java](file:///d:/Code/Java/forum/backend/src/main/java/com/hd/forum/entity/Post.java)。

### 5. `dto/` & `vo/` (数据传输层)
*   **【学术严谨版】**：**DTO (Data Transfer Object)** 用于入参封装，**VO (Value Object)** 用于结果展示视图封装。实现了内外层数据结构的彻底隔离，保护了内部物理模型的完整性与安全性。
*   **【简单易懂版】**：项目的**“定制快递盒”**。用户发过来的东西用 DTO 盒子装，返回给用户展示的东西用 VO 盒子装（比如把密码这种敏感东西拿掉，只给用户看昵称）。

### 6. `security/` & `config/` (安全与配置层)
*   **【学术严谨版】**：**系统基础设施配置层**。集成了 Spring Security 安全过滤器链，实现了基于无状态 JWT 的鉴权协议；`config` 包则负责 MyBatis Plus 分页插件、跨域 CORS 及静态资源映射的声明式配置。
*   **【简单易懂版】**：项目的**“安检站与中枢神经”**。它规定了谁能进门、谁能发帖，还规定了数据库地址在哪、文件存在哪。
*   **核心文件**：[JwtAuthenticationFilter.java](file:///d:/Code/Java/forum/backend/src/main/java/com/hd/forum/security/JwtAuthenticationFilter.java) (安检员)。

### 7. `aspect/` (切面层)
*   **【学术严谨版】**：基于 **AOP (Aspect Oriented Programming)** 范式，通过动态代理机制在不侵入业务逻辑的前提下，实现权限校验、日志审计等横切关注点的统一织入。
*   **【简单易懂版】**：项目的**“隐形监工”**。你发帖时它不出现，但如果你想删别人的帖子，它会瞬间跳出来查你是不是管理员。
*   **核心文件**：[AdminAspect.java](file:///d:/Code/Java/forum/backend/src/main/java/com/hd/forum/aspect/AdminAspect.java)。

---

## 二、 前端架构篇 (`frontend/src`)

前端基于 Vue 3 + Vite 构建，采用响应式与组件化开发模式。

### 1. `views/` (视图页面)
*   **【学术严谨版】**：**单页面应用 (SPA) 的路由终点组件**。承载了完整的页面生命周期管理，负责编排局部组件并调度数据请求 API。
*   **【简单易懂版】**：项目的**“全屏装修间”**。你在浏览器里看到的首页、详情页、登录页，每一个页面都对应这里的一个文件。
*   **核心文件**：[PostDetail.vue](file:///d:/Code/Java/forum/frontend/src/views/forum/PostDetail.vue)。

### 2. `components/` (基础组件)
*   **【学术严谨版】**：**原子化、可复用的 UI 单元**。遵循父子组件单向数据流原则，通过 `Props` 与 `Emits` 实现松耦合的交互通讯。
*   **【简单易懂版】**：项目的**“通用零件厂”**。比如一个漂亮的按钮、一个评论输入框，我把它们做成零件，在很多页面里都能直接搬过去用。

### 3. `api/` (网络请求层)
*   **【学术严谨版】**：**数据服务抽象层**。封装了 Axios 实例，将后端 RESTful 端点映射为前端可调用的异步函数，支持请求拦截与响应预处理。
*   **【简单易懂版】**：项目的**“电话拨号盘”**。这里记着后端每个办事窗口的电话（URL），前端想找后端要数据，直接在这里拨号就行。

### 4. `store/` (状态管理层)
*   **【学术严谨版】**：**全局状态持久化容器 (Pinia)**。实现了跨组件的数据共享与集中式状态流转，保证了用户信息在页面跳转过程中的状态一致性。
*   **【简单易懂版】**：项目的**“全局大仓库”**。存着你登录后的昵称、头像。不管你在首页还是详情页，大家都能随时去仓库里看看你是谁。

### 5. `utils/` (工具类层)
*   **【学术严谨版】**：**公用算法与底层封装库**。包含网络拦截器（Axios Interceptor）、日期格式化、Markdown 解析优化等通用函数。
*   **【简单易懂版】**：项目的**“多功能工具箱”**。比如怎么把一串数字变成日期、怎么给请求带上 Token，这些零碎活儿都在这里。

---

## 三、 根目录与资源篇

### 1. `knowledge_planet.sql`
*   **【学术严谨版】**：**数据库模式定义脚本 (DDL)**。包含 10 张核心实体的物理建表语句、主外键约束、唯一索引及初始化种子数据。
*   **【简单易懂版】**：项目的**“地基图纸”**。没有它，数据库里就是一片空白，系统根本跑不起来。

### 2. `uploads/`
*   **【学术严谨版】**：**服务器端文件存储上下文**。作为静态资源根目录，通过 WebMvc 配置映射为可供前端直接访问的 URL 路径。
*   **【简单易懂版】**：项目的**“储物间”**。用户上传的那些头像、帖子图片，最后全都被塞进了这个文件夹里。

---

## 💡 答辩核心逻辑总结

*   **如果老师问“代码怎么运行的？”**：
    > “后端请求从 `config/security` 拦截，进入 `controller` 接收，由 `service` 执行逻辑并调用 `mapper` 操作数据库；前端由 `views` 发起 `api` 请求，数据存入 `store` 后渲染在 `components` 中。”
*   **如果老师问“为什么这样设计？”**：
    > “这种前后端分离、后端 MVC 分层的设计，实现了**高内聚低耦合**，提高了系统的可维护性与扩展性。”
